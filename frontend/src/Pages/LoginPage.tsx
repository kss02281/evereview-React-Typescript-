import React, { Fragment } from 'react';
import { Navbar } from '../Components/common';

function LoginPage() {
  return (
    <Fragment>
    <Navbar />
      <h1>Login Page</h1>
    </Fragment>
  );
}

export default LoginPage;
